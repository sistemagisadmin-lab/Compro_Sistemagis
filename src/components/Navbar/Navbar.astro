---
import './navbar.css';
import SistemagisLogo from './SistemagisLogo.astro';

const currentPath = Astro.url.pathname.replace(/\/$/, '') || '/';

const desktopNavigation = [
  { name: 'Beranda', href: '/' },
  { name: 'Layanan', href: '/layanan' },
  { name: 'Produk', href: '/#products' },
  { name: 'Portofolio', href: '/#works' },
  { name: 'Tentang Kami', href: '/tentang-kami' },
];

const mobileNavigation = [
  {
    name: 'Beranda',
    href: '/',
    icon: `<path stroke-linecap="round" stroke-linejoin="round" d="M2.25 12l8.954-8.955a1.126 1.126 0 011.592 0L21.75 12M4.5 9.75v10.125c0 .621.504 1.125 1.125 1.125H9.75v-4.875c0-.621.504-1.125 1.125-1.125h2.25c.621 0 1.125.504 1.125 1.125V21h4.125c.621 0 1.125-.504 1.125-1.125V9.75M8.25 21h8.25" />`,
  },
  {
    name: 'Layanan',
    href: '/layanan',
    icon: `<path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6A2.25 2.25 0 016 3.75h2.25A2.25 2.25 0 0110.5 6v2.25a2.25 2.25 0 01-2.25 2.25H6a2.25 2.25 0 01-2.25-2.25V6zM3.75 15.75A2.25 2.25 0 016 13.5h2.25a2.25 2.25 0 012.25 2.25V18a2.25 2.25 0 01-2.25 2.25H6A2.25 2.25 0 013.75 18v-2.25zM13.5 6a2.25 2.25 0 012.25-2.25H18A2.25 2.25 0 0120.25 6v2.25A2.25 2.25 0 0118 10.5h-2.25a2.25 2.25 0 01-2.25-2.25V6zM13.5 15.75a2.25 2.25 0 012.25-2.25H18a2.25 2.25 0 012.25 2.25V18A2.25 2.25 0 0118 20.25h-2.25A2.25 2.25 0 0113.5 18v-2.25z" />`,
  },
  {
    name: 'Produk',
    href: '/#products',
    icon: `<path stroke-linecap="round" stroke-linejoin="round" d="M21 7.5l-9-5.25L3 7.5m18 0l-9 5.25m9-5.25v9l-9 5.25M3 7.5l9 5.25M3 7.5v9l9 5.25m0-9v9" />`,
  },
  {
    name: 'Portofolio',
    href: '/#works',
    icon: `<path stroke-linecap="round" stroke-linejoin="round" d="M6.429 9.75L2.25 12l4.179 2.25m11.142 0l4.179-2.25-4.179-2.25m-5.571 6.75l2.857-13.5" />`,
  },
  {
    name: 'Tentang Kami',
    href: '/tentang-kami',
    icon: `<path stroke-linecap="round" stroke-linejoin="round" d="M11.25 11.25l.041-.02a.75.75 0 011.063.852l-.708 2.836a.75.75 0 001.063.853l.041-.021M21 12a9 9 0 11-18 0 9 9 0 0118 0zm-9-3.75h.008v.008H12V8.25z" />`,
  },
];
---

<!-- Desktop & Mobile Unified Floating Navbar (Top) -->
<div id="navbar-root" class="fixed top-0 inset-x-0 z-50 pointer-events-none" data-current-path={currentPath}>
  <header class="py-2.5 sm:py-3 px-4 sm:px-6 md:px-8">
    <div class="w-full max-w-7xl mx-auto flex justify-center">
      <nav
        id="nav"
        class="pointer-events-auto flex items-center justify-between rounded-full"
        aria-label="Main Navigation"
      >
        <!-- Brand Logo Left (React Bits Shiny Text Effect) -->
        <a
          href="/"
          class="brand-logo-wrap relative flex items-center select-none shrink-0 group cursor-pointer"
          aria-label="Sistemagis Home"
        >
          <SistemagisLogo class="h-5 sm:h-6 w-auto block" theme="dark" idPrefix="nav-logo" />
        </a>

        <!-- Middle Floating Nav Links (Visible on desktop md+) -->
        <div class="nav-links-container relative hidden md:flex items-center">
          <div class="nav-pointer"></div>
          {
            desktopNavigation.map((item) => {
              const isActive = (item.href === currentPath) || (item.href !== '/' && currentPath.startsWith(item.href) && !item.href.includes('#'));
              return (
                <a
                  href={item.href}
                  class={`nav-item relative z-10 py-1 px-3 lg:px-3.5 text-xs sm:text-sm font-semibold transition-colors duration-200 cursor-pointer select-none ${isActive ? 'active' : ''}`}
                  data-href={item.href}
                >
                  {item.name}
                </a>
              );
            })
          }
        </div>

        <!-- Right Action CTA: "Konsultasi gratis" -->
        <a
          href="/#contact"
          class="inline-flex items-center gap-1.5 sm:gap-2 px-3.5 sm:px-4 py-1.5 sm:py-2 rounded-full text-xs sm:text-sm font-bold text-white bg-[#5194ea] hover:bg-[#3f7cd0] shadow-sm shadow-[#5194ea]/25 transition-all hover:scale-105 shrink-0"
        >
          <span>Konsultasi gratis</span>
          <svg class="w-3.5 h-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
            <path stroke-linecap="round" stroke-linejoin="round" d="M13.5 4.5L21 12m0 0l-7.5 7.5M21 12H3" />
          </svg>
        </a>
      </nav>
    </div>
  </header>
</div>

<!-- Mobile Floating App Bottom Dock Navigation -->
<div id="mobile-nav-root" class="fixed bottom-5 inset-x-0 z-50 pointer-events-none flex justify-center px-4 md:hidden">
  <nav
    id="mobile-nav"
    class="pointer-events-auto inline-flex items-center justify-center gap-1.5 rounded-full p-2 shadow-2xl backdrop-blur-2xl theme-cyan"
    aria-label="Mobile Navigation"
  >
    {
      mobileNavigation.map((item) => (
        <a
          href={item.href}
          aria-label={item.name}
          title={item.name}
          class="mobile-tab relative flex items-center justify-center w-11 h-11 rounded-full transition-all duration-300 select-none cursor-pointer group"
          data-href={item.href}
        >
          <div class="mobile-icon-wrapper relative z-10 flex items-center justify-center transition-transform duration-300">
            <svg
              class="w-5 h-5 transition-colors duration-300"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
              stroke-width="2"
              set:html={item.icon}
            />
          </div>
          <div class="mobile-active-pill absolute inset-0 rounded-full opacity-0 scale-75 transition-all duration-300 z-0"></div>
        </a>
      ))
    }
  </nav>
</div>

<script>
  import { initNavbar } from './navbarTransitions';

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initNavbar);
  } else {
    initNavbar();
  }
</script>
